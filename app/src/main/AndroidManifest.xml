<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackathon.hackathon_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:screenOrientation="sensorPortrait"
        android:label="@string/app_name"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hackathon">
        <activity android:name=".UI.AboutUs"></activity>
        <activity android:name=".UI.MentorInfo" />
        <activity android:name=".UI.Fag" />
        <activity android:name=".UI.ChatRoom" />
        <activity android:name=".UI.Tags" />
        <activity android:name=".UI.ListMentors" />
        <activity android:name=".Auth" />
        <activity android:name=".UI.SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>